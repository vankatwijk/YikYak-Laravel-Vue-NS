android {
            defaultConfig {
                applicationId = "org.nativescript.playvue"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }